spring:
  datasource:
    url: jdbc:mysql://localhost/tasktracker
    username: root
    password: root          
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
security:
  user:
    password: admin            
logging:
  level:
    org.springframework.security: DEBUG